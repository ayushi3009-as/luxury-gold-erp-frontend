import React from "react";


export default function GoodsReceiptLayout({
    children,
}: {
    children: React.ReactNode;
}) {


    return (

        <section>

            {children}

        </section>

    );

}